<UserControl x:Class="LATravelManager.UI.Views.ThirdParty.NewReservation_ThirdParty_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LATravelManager.UI.Views.ThirdParty"
             xmlns:helpers="clr-namespace:LATravelManager.UI.Helpers"
             xmlns:converters="clr-namespace:LATravelManager.UI.Converters"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:viewmodels="clr-namespace:LATravelManager.UI.ViewModel.CategoriesViewModels.ThirdParty"
             d:DataContext="{d:DesignInstance viewmodels:NewReservation_ThirdParty_VIewModel}"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="700"
             d:DesignWidth="1200">
    <UserControl.Resources>
        <converters:BoolToColorConverter x:Key="BrushColorConverter" />
        <converters:UserToVisibility x:Key="UserToVisibilityConverter" />
        <converters:FloadToStringValueConverter x:Key="FtoSConverter" />
        <converters:RoomsLeftToVisibilityConverter x:Key="RoomsLeftToVisibilityConverter" />
        <converters:RoomTypeToRoomsLeftConverter x:Key="RoomTypeToRoomsLeftConverter" />
        <converters:SelectedIndexToForegroundConverter x:Key="SelectedIndexToForegroundConverter" />
        <Style TargetType="{x:Type Border}">
            <Setter Property="BorderBrush"
                    Value="Gray" />
            <Setter Property="BorderThickness"
                    Value="1,1,0,1" />
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="HorizontalAlignment"
                    Value="Center" />
        </Style>
        <Style x:Key="RowStyle"
               TargetType="{x:Type DataGridRow}">
            <Setter Property="IsSelected"
                    Value="{Binding IsSelected, Mode=TwoWay,
                             UpdateSourceTrigger=PropertyChanged}" />
        </Style>
        <Style x:Key="DataGridCellStyle"
               TargetType="{x:Type DataGridCell}">
            <Setter Property="IsTabStop"
                    Value="False" />
        </Style>
        <DataTemplate x:Key="EnabledTemplate"
                      DataType="{x:Type viewmodels:NewReservation_ThirdParty_VIewModel}">
            <StackPanel>
                <Button Content="Ακύρωση Κράτησης"
                        Padding="3 3"
                        FontSize="11"
                        VerticalAlignment="Top"
                        Margin="0 5 0 1"
                        Command="{Binding ToggleDisabilityCommand}"
                        Visibility="{Binding ConverterParameter=1, Converter={StaticResource UserToVisibilityConverter}}"
                        HorizontalAlignment="Center" />
                <TextBlock Text="Λόγος:" />
                <TextBox HorizontalAlignment="Center"
                         Width="140"
                         Text="{Binding ThirdPartyWr.CancelReason,UpdateSourceTrigger=PropertyChanged}"
                         Height="43"
                         TextWrapping="Wrap" />
            </StackPanel>
        </DataTemplate>
        <DataTemplate x:Key="DisabledTemplate"
                      DataType="{x:Type viewmodels:NewReservation_ThirdParty_VIewModel}">
            <Grid Background="Red">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Ακυρώθηκε"
                           Margin=" 5 0 0 0"
                           HorizontalAlignment="Center" />
                <Button Content="Επαναφορά"
                        Visibility="{Binding ConverterParameter=1, Converter={StaticResource UserToVisibilityConverter}}"
                        Grid.Row="0"
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Command="{Binding ToggleDisabilityCommand}"
                        Padding="3 0"
                        HorizontalAlignment="Center" />
                <StackPanel Orientation="Horizontal"
                            Margin=" 5 0 0 0"
                            Grid.Row="1"
                            Grid.ColumnSpan="2"
                            Grid.Column="0">
                    <TextBlock Text="{Binding ThirdPartyWr.DisableDate,StringFormat='{}{0:dd/MM/yyyy HH:mm -}' }"
                               FontSize="11"
                               TextWrapping="Wrap" />
                    <Label Content="{Binding ThirdPartyWr.DisabledBy.UserName}"
                           Padding="5 0" />
                </StackPanel>
                <TextBox HorizontalAlignment="Center"
                         Width="130"
                         Grid.Row="2"
                         Grid.ColumnSpan="2"
                         Text="{Binding ThirdPartyWr.CancelReason,UpdateSourceTrigger=PropertyChanged}"
                         Height="45"
                         Background="AntiqueWhite"
                         TextWrapping="Wrap" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        <Grid VerticalAlignment="Top"
              HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <FrameworkElement x:Name="dummyElement"
                              Visibility="Collapsed" />
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="5,0">
                    <DataGrid Margin="5"
                              FontSize="12"
                              Height="250"
                              Width="640"
                              CanUserSortColumns="False"
                              LoadingRow="DataGrid_LoadingRow"
                              PreparingCellForEdit="CustomersDataGrid_PreparingCellForEdit"
                              x:Name="CustomersDataGrid"
                              CanUserDeleteRows="False"
                              KeyUp="DataGrid_KeyDown"
                              HorizontalAlignment="Left"
                              ItemsSource="{Binding ThirdPartyWr.CustomerWrappers, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              AutoGenerateColumns="False"
                              SelectedItem="{Binding SelectedCustomer, TargetNullValue={x:Static CollectionView.NewItemPlaceholder}}">
                        <DataGrid.Resources>
                            <Style TargetType="{x:Type ComboBox}">
                                <Setter Property="Margin"
                                        Value="0" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Διαγραφη"
                                          Command="{Binding DeleteSelectedCustomersCommand}">
                                    <MenuItem.Icon>
                                        <Image MaxHeight="20"
                                               Stretch="Fill" />
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.RowStyle>
                            <StaticResource ResourceKey="RowStyle" />
                        </DataGrid.RowStyle>
                        <DataGrid.RowValidationRules>
                            <helpers:RowDataInfoValidationRule ValidationStep="UpdatedValue" />
                        </DataGrid.RowValidationRules>
                        <DataGrid.RowHeaderStyle>
                            <Style TargetType="{x:Type DataGridRowHeader}">
                                <Setter Property="Width"
                                        Value="20" />
                            </Style>
                        </DataGrid.RowHeaderStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Δωμάτιο"
                                                IsReadOnly="True"
                                                Binding="{Binding RoomNumber, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="{x:Type DataGridCell}">
                                        <Setter Property="Background"
                                                Value="{Binding RoomColor, Mode=TwoWay, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" />
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Όνομα"
                                                MaxWidth="200"
                                                MinWidth="90"
                                                Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Επίθετο"
                                                MinWidth="90"
                                                MaxWidth="200"
                                                Binding="{Binding Surename, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTextColumn Header="Τηλέφωνο"
                                                MinWidth="80"
                                                MaxWidth="200"
                                                Binding="{Binding Tel, UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTemplateColumn Header="Αναχώρηση"
                                                    Width="100"
                                                    CellStyle="{StaticResource DataGridCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox IsEditable="True"
                                                  Width="Auto"
                                                  VerticalAlignment="Top"
                                                  MinWidth="60"
                                                  Text="{Binding StartingPlace, UpdateSourceTrigger=PropertyChanged}"
                                                  ItemsSource="{x:Static helpers:StaticResources.StartingPlaces}"
                                                  DisplayMemberPath="Name" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Γέννηση"
                                                    CellStyle="{StaticResource DataGridCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DatePicker Width="Auto"
                                                    VerticalAlignment="Center"
                                                    MinWidth="60"
                                                    SelectedDate="{Binding DOB,UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Passport Id"
                                                MaxWidth="200"
                                                Binding="{Binding PassportNum,UpdateSourceTrigger=PropertyChanged}" />
                            <DataGridTemplateColumn Header="Έκδοση"
                                                    Width="Auto"
                                                    CellStyle="{StaticResource DataGridCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DatePicker VerticalAlignment="Center"
                                                    Width="95"
                                                    SelectedDate="{Binding PassportPublish, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Λήξη"
                                                    Width="Auto"
                                                    CellStyle="{StaticResource DataGridCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DatePicker VerticalAlignment="Center"
                                                    Width="95"
                                                    SelectedDate="{Binding PassportExpiration, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Email"
                                                MaxWidth="100"
                                                Binding="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                                />
                            <DataGridTemplateColumn Header="Πάσο"
                                                    Width="70">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Name="test"
                                                  SelectedIndex="{Binding DeserveDiscount,UpdateSourceTrigger=PropertyChanged}"
                                                  Foreground="{Binding RelativeSource={RelativeSource Self},Path=SelectedIndex,ConverterParameter=0, Converter={StaticResource SelectedIndexToForegroundConverter},UpdateSourceTrigger=PropertyChanged}">
                                            <ComboBoxItem Content="ΟΧΙ" />
                                            <ComboBoxItem Content="ΦΟΙΤΗΤ." />
                                            <ComboBoxItem Content="ΠΟΛΥΤΕΚΝ.." />
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <TextBlock Text="{Binding ErrorsInDatagrid}"
                                   FontSize="14"
                                   Foreground="Red"
                                   MaxHeight="100"
                                   MaxWidth="500" />
                        <TextBlock x:Name="BookedMessage"
                                   Grid.Row="2"
                                   FontSize="14"
                                   Foreground="Green"
                                   MaxWidth="390"
                                   MaxHeight="60"
                                   HorizontalAlignment="Right"
                                   Text="{Binding BookedMessage}" />
                    </StackPanel>
                </StackPanel>
                <GroupBox Grid.Column="1"
                          Header="Πληρωμές"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Top">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal"
                                    Background="AliceBlue">
                            <GroupBox HorizontalAlignment="Left"
                                      VerticalAlignment="Top"
                                      Header="Νέα Πληρωμή">
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel Margin="5,5,5,0"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="Ποσό:"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center" />
                                        <TextBox Grid.Row="0"
                                                 HorizontalAlignment="Center"
                                                 Grid.Column="1"
                                                 Text="{Binding Payment.Amount,Converter={StaticResource FtoSConverter},UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                                                 Width="50" />
                                        <CheckBox Content="Out."
                                                  Margin=" 0 5 0 0"
                                                  Visibility="{Binding ConverterParameter=1, Converter={StaticResource UserToVisibilityConverter}}"
                                                  IsChecked="{Binding Payment.Outgoing}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1"
                                                Margin="5">
                                        <TextBlock Text="Μέθοδος:"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center" />
                                        <ComboBox Grid.Row="0"
                                                  Margin="0,5"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  SelectedIndex="{Binding Payment.PaymentMethod,NotifyOnValidationError=True}"
                                                  Grid.Column="1"
                                                  Width="80">
                                            <ComboBoxItem>Μετρητά</ComboBoxItem>
                                            <ComboBoxItem>Πειραιώς</ComboBoxItem>
                                            <ComboBoxItem>Εθνική</ComboBoxItem>
                                            <ComboBoxItem>Eurobank</ComboBoxItem>
                                            <ComboBoxItem>Alpha Bank</ComboBoxItem>
                                            <ComboBoxItem>VISA</ComboBoxItem>
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Row="1"
                                                Margin="5,5,0,0"
                                                Grid.Column="0"
                                                HorizontalAlignment="Stretch"
                                                Grid.ColumnSpan="2">
                                        <TextBlock Text="Σημείωση Πληρωμής:"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center" />
                                        <TextBox Grid.Row="0"
                                                 Margin="0,0,5,-5"
                                                 HorizontalAlignment="Stretch"
                                                 Grid.Column="1"
                                                 Width="120"
                                                 TextWrapping="Wrap"
                                                 Height="40"
                                                 Text="{Binding Payment.Comment}" />
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                            <Border VerticalAlignment="Stretch"
                                    Width="140"
                                    HorizontalAlignment="Center"
                                    BorderThickness="0">
                                <ContentControl Content="{Binding }">
                                    <ContentControl.Style>
                                        <Style TargetType="{x:Type ContentControl}">
                                            <Setter Property="ContentTemplate"
                                                    Value="{StaticResource  DisabledTemplate}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ThirdPartyWr.Disabled }"
                                                             Value="false">
                                                    <Setter Property="ContentTemplate"
                                                            Value="{StaticResource EnabledTemplate}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Row="1"
                                    Orientation="Vertical">
                            <TextBlock Text=" Ιστορικό Πληρωμών"
                                       HorizontalAlignment="Left" />
                            <ListView Name="payments"
                                      Focusable="False"
                                      Height="142"
                                      SelectedItem="{Binding SelectedPayment}"
                                      Width="425"
                                      ItemsSource="{Binding ThirdPartyWr.Payments}">
                                <ListView.Resources>
                                    <ContextMenu x:Key="ItemContextMenu">
                                        <MenuItem Header="Διαγραφη"
                                                  Visibility="{Binding Converter={StaticResource UserToVisibilityConverter},ConverterParameter=1,  Mode=OneWay}"
                                                  Command="{Binding DataContext.DeletePaymentCommand,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}">
                                            <MenuItem.Icon>
                                                <Image MaxHeight="20"
                                                       Stretch="Fill" />
                                            </MenuItem.Icon>
                                        </MenuItem>
                                        <MenuItem Header="Εκτ. Απόδειξης"
                                                  Command="{Binding DataContext.PrintRecieptCommand,RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListView}}">
                                            <MenuItem.Icon>
                                                <Image MaxHeight="20"
                                                       Stretch="Fill" />
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </ListView.Resources>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="ContextMenu"
                                                Value="{StaticResource ItemContextMenu}" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Width="28"
                                                        Header="Out.">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <CheckBox IsChecked="{Binding Outgoing}"
                                                              IsHitTestVisible="False" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Ημερομηνία"
                                                        Width="115"
                                                        DisplayMemberBinding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy-HH:mm}'}" />
                                        <GridViewColumn Header="Ποσό"
                                                        Width="40"
                                                        DisplayMemberBinding="{Binding Amount,Converter={StaticResource FtoSConverter}}" />
                                        <GridViewColumn Header="Μέθοδος"
                                                        Width="70" />
                                        <GridViewColumn Header="Χρήστης"
                                                        Width="80"
                                                        DisplayMemberBinding="{Binding User.UserName}" />
                                        <GridViewColumn Header="Σημείωση"
                                                        DisplayMemberBinding="{Binding Comment}" />
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </Grid>
                </GroupBox>
            </Grid>
            <StackPanel Orientation="Horizontal"
                        Grid.Row="1">
                <GroupBox Header="Περιγραφή Πακέτου"
                          VerticalAlignment="Top">
                    <StackPanel>
                        <StackPanel.Resources>
                            <Style TargetType="ComboBox">
                                <Setter Property="MinWidth"
                                        Value="50" />
                                <Setter Property="Width"
                                        Value="110" />
                                <Setter Property="VerticalAlignment"
                                        Value="Center" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel>
                                    <Label Content="Check In:"
                                           Padding="0" />
                                    <DatePicker SelectedDate="{Binding ThirdPartyWr.CheckIn}"
                                                Margin="0,0,5,0" />
                                </StackPanel>
                                <StackPanel Margin="0,0,5,0">
                                    <Label Content="Check Out:"
                                           Padding="0" />
                                    <DatePicker SelectedDate="{Binding ThirdPartyWr.CheckOut}" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal"
                                        Margin="0"
                                        HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding ThirdPartyWr.DatesError}"
                                           TextWrapping="Wrap"
                                           Foreground="Red"
                                           Width="138" />
                            </StackPanel>
                            <TextBlock Text="Περιγραφή πακέτου:"
                                       HorizontalAlignment="Left" />
                            <TextBox HorizontalAlignment="Stretch"
                                     TextWrapping="Wrap"
                                     Height="65"
                                     Margin=" 0 0 0 5"
                                     Text="{Binding ThirdPartyWr.Description,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                     VerticalAlignment="Bottom" />
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Προορισμός:"
                                           Margin="0 0 5 0"
                                           HorizontalAlignment="Left" />
                                <TextBox HorizontalAlignment="Left"
                                         Margin="0"
                                         Grid.Column="1"
                                         MinWidth="160"
                                         Text="{Binding ThirdPartyWr.City,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                         VerticalAlignment="Bottom" />
                                <TextBlock Text="Διαμονή:"
                                           Grid.Row="1"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left" />
                                <TextBox HorizontalAlignment="Left"
                                         Margin="0 5 "
                                         Grid.Column="1"
                                         Grid.Row="1"
                                         MinWidth="160"
                                         Text="{Binding ThirdPartyWr.Stay,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                         VerticalAlignment="Bottom" />
                            </Grid>
                            <Button Content="Προσθήκη/Αλλαγή Αρχείου:"
                                    Margin="0 5 5 5"
                                    HorizontalAlignment="Left"
                                    Command="{Binding UploadFileCommand}" />
                            <StackPanel Orientation="Horizontal">
                                <TextBlock HorizontalAlignment="Stretch"
                                           Text="Αρχείο:"
                                           VerticalAlignment="Bottom" />
                                <TextBlock HorizontalAlignment="Stretch"
                                           Margin=" 5 0"
                                           VerticalAlignment="Bottom">
                                    <Hyperlink Command="{Binding OpenFileCommand}">
                                        <TextBlock Text="{Binding ThirdPartyWr.File.FileName}" />
                                    </Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                <Grid HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Margin="5"
                      Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <GroupBox Margin="5,0"
                              Width="195"
                              VerticalAlignment="Top"
                              Header="Κράτηση">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal"
                                        Margin="0,5,0,0">
                                <ComboBox Width="140"
                                          ItemsSource="{Binding Partners}"
                                          SelectedIndex="{Binding SelectedPartnerIndex,UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                                          DisplayMemberPath="Name"
                                          VerticalAlignment="Center"
                                          Margin="0,0,5,5" />
                                <Button Cursor="Hand"
                                        VerticalAlignment="Center"
                                        Command="{Binding UpdateAllCommand}"
                                        HorizontalAlignment="Right">
                                    <iconPacks:PackIconFontAwesome Kind="SyncSolid"
                                                                   VerticalAlignment="Center"
                                                                   Margin="2" />
                                </Button>
                            </StackPanel>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Σύνολο:"
                                           HorizontalAlignment="Left" />
                                <TextBox Text="{Binding ThirdPartyWr.FullPrice,Converter={StaticResource FtoSConverter},UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                                         Margin="5,0,5,5"
                                         VerticalAlignment="Center"
                                         MinWidth="40"
                                         MaxWidth="60"
                                         Grid.Column="1" />
                                <TextBlock Text="Προμήθεια:"
                                           Grid.Row="1"
                                           HorizontalAlignment="Left" />
                                <TextBox Text="{Binding ThirdPartyWr.Commision,Converter={StaticResource FtoSConverter},UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                                         Margin="5,0,5,5"
                                         VerticalAlignment="Center"
                                         MinWidth="40"
                                         MaxWidth="60"
                                         Grid.Row="1"
                                         Grid.Column="1" />
                                <TextBlock Text="Νet τιμή:"
                                           Grid.Row="2"
                                           HorizontalAlignment="Left" />
                                <TextBox Text="{Binding ThirdPartyWr.NetPrice,Converter={StaticResource FtoSConverter},UpdateSourceTrigger=PropertyChanged,ValidatesOnDataErrors=True}"
                                         Margin="5,0,5,5"
                                         VerticalAlignment="Center"
                                         MinWidth="40"
                                         MaxWidth="60"
                                         Grid.Row="2"
                                         Grid.Column="1" />
                            </Grid>
                            <Border Margin="-5,5"
                                    BorderThickness="0,1,0,0"
                                    BorderBrush="#FFD5DFE5" />
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Label Content="Προκαταβολή:"
                                       Grid.Row="3"
                                       Grid.Column="0"
                                       Margin="0,0,5,0" />
                                <TextBlock Grid.Row="3"
                                           VerticalAlignment="Center"
                                           Width="50"
                                           Grid.Column="1"
                                           Text="{Binding ThirdPartyWr.Recieved,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource FtoSConverter}}"
                                           Margin="0,0,5,0" />
                                <Label Content="Υπόλ. Πελάτη:"
                                       Grid.Row="5"
                                       Grid.Column="0"
                                       Margin="0,0,5,0" />
                                <TextBlock Grid.Row="5"
                                           Width="50"
                                           Background="LightGreen"
                                           Text="{Binding ThirdPartyWr.Remaining,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource FtoSConverter}}"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,5,0" />
                                <Label Content="Οφειλή μας:"
                                       Grid.Row="4"
                                       Grid.Column="0"
                                       Margin="0,0,5,0" />
                                <TextBlock Grid.Row="4"
                                           Width="50"
                                           Text="{Binding ThirdPartyWr.NetRemaining,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource FtoSConverter}}"
                                           VerticalAlignment="Center"
                                           Grid.Column="1"
                                           Margin="0,0,5,0" />
                                <TextBlock Text="Σημείωση Κράτησης:"
                                           Grid.Row="6"
                                           Margin="5 0"
                                           HorizontalAlignment="Left"
                                           Grid.ColumnSpan="2" />
                                <TextBox Grid.Row="7"
                                         Grid.ColumnSpan="2"
                                         HorizontalAlignment="Center"
                                         TextWrapping="Wrap"
                                         Height="65"
                                         Margin="5 0"
                                         Width="173"
                                         Text="{Binding ThirdPartyWr.Comment,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                         VerticalAlignment="Bottom" />
                            </Grid>
                            <ComboBox ItemsSource="{Binding Users}"
                                      SelectedIndex="{Binding SelectedUserIndex}"
                                      DisplayMemberPath="UserName"
                                      Margin="5"
                                      Visibility="{Binding Converter={StaticResource UserToVisibilityConverter},ConverterParameter=1,  Mode=OneWay}"
                                      VerticalAlignment="Center" />
                            <TextBlock Text="{Binding ErrorsInCanAddReservationToRoom}"
                                       HorizontalAlignment="Center"
                                       FontSize="14"
                                       Foreground="Red"
                                       TextWrapping="Wrap"
                                       MaxHeight="100"
                                       MaxWidth="150" />
                        </StackPanel>
                    </GroupBox>
                </Grid>
                <StackPanel Grid.Column="1"
                            HorizontalAlignment="Left">
                    <Border BorderThickness="1"
                            Visibility="Visible"
                            VerticalAlignment="Top"
                            Margin="0,10">
                        <Grid Margin="5">
                            <Grid.Resources>
                                <Style TargetType="Label">
                                    <Setter Property="HorizontalAlignment"
                                            Value="Center" />
                                    <Setter Property="Padding"
                                            Value="0,5,0,0" />
                                </Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment"
                                            Value="Center" />
                                    <Setter Property="Background"
                                            Value="LightGray" />
                                </Style>
                            </Grid.Resources>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0"
                                        VerticalAlignment="Top">
                                <Label Content="Πωλητής:"
                                       Padding="0"
                                       HorizontalAlignment="Center" />
                                <TextBlock Text="{Binding ThirdPartyWr.User.UserName }" />
                                <Label Content="Προστέθηκε:"
                                       Padding="0" />
                                <TextBlock Text="{Binding ThirdPartyWr.CreatedDate, StringFormat='{}{0:dd/MM/yyyy}' }" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Button Content="Αποθήκευση"
                            Margin="5,5,5,0"
                            Command="{Binding SaveCommand}" />
                    <Button Content="Προσθήκη πελάτη"
                            Margin="5,5,5,0"
                            Visibility="{Binding ConverterParameter=1, Converter={StaticResource UserToVisibilityConverter}}"
                            Command="{Binding AddCustomerCommand}" />
                    <!--<Button Content="Προσθήκη από αρχείο"
                            Margin="5,5,5,0"
                            Visibility="{Binding ConverterParameter=1, Converter={StaticResource UserToVisibilityConverter}}"
                            Command="{Binding AddFromFileCommand}" />-->
                    <Button Content="Read Next Line"
                            Margin="5,5,5,0" />
                    <Button Content="Καθαρισμός"
                            Margin="5,5,5,0"
                            Command="{Binding ClearBookingCommand}" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>