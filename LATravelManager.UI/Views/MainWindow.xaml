<Window x:Class="LATravelManager.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Language="el-Gr"
        xmlns:viewmodels="clr-namespace:LATravelManager.UI.ViewModel.Window_ViewModels"
        xmlns:views="clr-namespace:LATravelManager.UI.Views"
        d:DataContext="{d:DesignInstance viewmodels:MainViewModel}"
        Height="840"
        Width="1335"
        Closed="Window_Closed"
        WindowStartupLocation="CenterScreen"
       Initialized="Window_Initialized">
    <Window.IsEnabled>
        <Binding Path="BindableProperty"
                 Source="{StaticResource MessageListener}" />
    </Window.IsEnabled>
    <Window.Visibility>
        <Binding Path="Visibility" />
    </Window.Visibility>
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <views:Login_UserControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MainUserControl_ViewModel}">
            <views:Main_UserControl />
        </DataTemplate>
    </Window.Resources>

        <ContentControl Content="{Binding SelectedViewmodel}" Grid.Row="1" />
</Window>